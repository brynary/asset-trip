require "spec_helper"

describe AssetTrip::Stylesheet do
  describe "#contents" do
    it "rewrites the URLs"
    it "compresses the contents"
    it "only runs the Compressor once for the package"
  end
end
